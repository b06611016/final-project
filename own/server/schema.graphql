type Query {
    userCheck(username: String!, password: String!): Boolean!
}

type Mutation {
    createUser(username: String!, password: String!): Boolean!
}

enum MutationType {
    CREATED
    DELETED
    CLEAR
}